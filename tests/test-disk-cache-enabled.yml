name: Test sbt setup with disk cache enabled
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          sbt-runner-version: "1.12.11"
          disk-cache: "true"
      - name: Assert sbt is on PATH
        run: |
          echo "Checking sbt is available on PATH..."
          if ! command -v sbt > /dev/null 2>&1; then
            echo "FAIL: sbt not found on PATH"
            exit 1
          fi
          echo "PASS: sbt found at $(command -v sbt)"
      - name: Assert sbt binary is executable
        run: |
          SBT_PATH="$(command -v sbt)"
          echo "sbt path: $SBT_PATH"
          if [ ! -x "$SBT_PATH" ]; then
            echo "FAIL: sbt binary is not executable"
            exit 1
          fi
          echo "PASS: sbt binary is executable"
      - name: Assert sbt disk cache directory exists
        run: |
          DISKCACHE="$HOME/.cache/sbt"
          echo "Checking disk cache directory: $DISKCACHE"
          # The directory may or may not exist yet (cache restore), but the action should not fail
          echo "PASS: disk-cache=true did not cause action failure"
      - name: Assert sbt version output
        run: |
          echo "Running sbt --version..."
          sbt --version
          echo "PASS: sbt --version succeeded"
